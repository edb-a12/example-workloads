[
    {
        "type": "string",
        "key": "name",
        "options": {
            "default": "set-a-name-please"
        },
        "description": "NAME!!!"
    },
    {
        "type": "string",
        "key": "cpu",
        "options": {
            "default": "100m"
        },
        "description": "CPU limit for the container"
    },
    {
        "type": "number",
        "key": "replicas",
        "options": {
            "default": 1
        },
        "description": "Number of replicas to run"
    },
    {
        "type": "string",
        "key": "envVars",
        "options": {
            "default": "key=value\nkey2=value2"
        },
        "description": "Environment variables to set in the container"
    }
]